package com.lw.bapm.listener

import com.lw.bapm.base.IMonitor
import com.lw.bapm.base.IMonitorListener

class TimeMonitorListener : IMonitorListener {
    override fun onInit(monitor: IMonitor) {
    }

    override fun onStart(monitor: IMonitor) {
    }

    override fun onStop(monitor: IMonitor) {
    }

    override fun onDestory(monitor: IMonitor) {
    }

    override fun onForground(monitor: IMonitor, isForground: Boolean) {
    }
}